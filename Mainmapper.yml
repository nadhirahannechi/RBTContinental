# variable_mapping.yml
# Ce fichier contient les variables envoyées par ServiceNow.

job_template_id: "{{ job_template_id }}"  # ID de job_template reçu de ServiceNow
variable1: "{{ variable1 }}"                # Variable envoyée par ServiceNow
variable2: "{{ variable2 }}"
variable3: "{{ variable3 }}"
variable4: "{{ variable4 }}"
variable5: "{{ variable5 }}"
